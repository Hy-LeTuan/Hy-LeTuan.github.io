.footer {
	margin-top: var(--gap-md);
	align-items: center;
	justify-content: space-between;
	padding: var(--gap-6) var(--gap-lg);
	font-size: var(--fs-0);
	border-top: 1px solid var(--color-primary-light);

    .footer__main {
		display: flex;
        flex-direction: row;
		width: 100%;
        justify-content: space-around;

		.footer__main__col {
          flex: 1 1 0;
          h5 {
            color: var(--color-accent-orange);
          }
		}
	}
}
